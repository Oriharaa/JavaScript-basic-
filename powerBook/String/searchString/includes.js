/*
  includes() : 특정 문자열이 있으면 true, 없으면 false를 반환.
               (특정 문자열이 포함되었는지 여부만을 판단.)
*/

    const str1 = 'good morning, good afternoon, good evening, and good night';

    console.log(str1.includes('even')); //true 
    console.log(str1.includes('dawn')); //false